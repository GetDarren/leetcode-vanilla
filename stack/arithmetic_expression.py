  """
  [Calculate the arithmetic expression]
  i.e.  ( 1+( (2+3) * (4*5) ) )
  hint:  two stacks,  one store the values,  another one stores the operators;
  if encount "(" then pass, if encount ")", then start calculate the pop element from two stacks 
  """
  